# 이 저장소는 공개다. prop-dashboard 에는 실계좌 보유종목·평가금액·손익이 들어 있어
# 별도 비공개 저장소(pdhman/prop-dashboard)에서 관리한다. 여기에는 절대 커밋 금지.
# (자체 .git 을 가진 폴더라 실수로 add 하면 gitlink 로 들어간다 — 아예 무시한다)
prop-dashboard/

# 서비스 계정 키 등 비밀값 (절대 커밋 금지)
gdrive_sa.json
*.json
!package.json
!reports/counts.json
!x-monitor/accounts.json
!x-monitor/data/*.json
!reports/data/*.json

# 데이터 파일 (드라이브에서 관리됨)
*.xlsx
# 예외: 스크리너 결과는 러너의 요약/차트 생성에 필요해 저장소에 포함
!종목탐색_TOP30.xlsx

# 실행 로그
logs/
*.tmp

# 레버리지 수집기: 격리 의존성·SQLite(재생성 가능). CSV 는 시계열이라 추적한다
market_leverage_collector/.deps/
market_leverage_collector/data/*.db

# Python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# OS / 에디터
.DS_Store
Thumbs.db
market_leverage_collector/leverage.html

# OpenDART 조회 캐시
.dart_cache/

# API 키 등 개인 설정 (절대 커밋 금지)
config.yaml

# price-alert: 카카오 토큰·알림 상태 (절대 커밋 금지)
price-alert/kakao_tokens.json
price-alert/alert_state.json
!price-alert/config.json
