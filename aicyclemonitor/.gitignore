# generated / local
# (히스토리 CSV·manual_data.json은 GitHub Actions 주간 실행이 누적·공유하도록 repo에 포함)
dashboard.html
monitor_log.txt
shot_*.png
__pycache__/

# 루트 .gitignore 의 전역 *.json 규칙 예외 — 수동 입력 데이터는 repo에 포함
!manual_data.json
