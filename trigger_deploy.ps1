# =====================================================================
# (은퇴) GitHub Pages 배포 트리거 — 2026-08-22 브랜치 서빙 전환으로 불필요
#  - 사이트가 GitHub Pages "Deploy from a branch"(main /docs)로 바뀌어
#    push 자체가 배포다. Actions 배포 잡도, 이 명시적 트리거도 필요 없다.
#  - run_*.ps1 7곳이 여전히 push 직후 이 스크립트를 호출하므로, 호출부를
#    일일이 고치는 대신 no-op 으로 남겨 둔다(항상 exit 0).
# =====================================================================
exit 0
